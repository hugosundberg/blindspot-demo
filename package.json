{
  "name": "blindspot",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "dev:legacy": "react-scripts start",
    "build:legacy": "react-scripts build",
    "server:legacy": "node server/index.js",
    "server:dev:legacy": "node --watch server/index.js"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=18"
  }
}
